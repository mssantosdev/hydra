---
description: Break the active epic into executable tasks, dependencies, and parallel work lanes.
agent: manager
subtask: true
---
Read the active epic docs and produce a concrete execution breakdown.

Use `tasks.md` as the source of truth for executable lanes.

Include:

1. tasks
2. dependencies
3. what can run in parallel
4. which role should handle each step
5. risks or conflict hotspots
